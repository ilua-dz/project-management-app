import { Footer } from 'antd/lib/layout/layout';
import styled from 'styled-components';

const FooterApp = () => <StyledFooter>footer content</StyledFooter>;

const StyledFooter = styled(Footer)`
  text-align: center;
`;

export { FooterApp };
